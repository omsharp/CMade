#ifndef MODULE_B_H
#define MODULE_B_H
#include <stdlib.h>

/**
 * A function that takes an array of int,
 * and returns the smallest number in it.
 */
int find_min_int(const int *array, size_t len);

#endif